{
  "name": "01som",
  "version": "0.1.0",
  "description": "0 + 1 = SOM",
  "scripts": {
    "sass": "sass --sourcemap=none --no-cache --watch . --style compressed",
    "pug": "pug S1 S2 S3 -w",
    "server": "http-server -c-1 ."
  },
  "private": true
}
