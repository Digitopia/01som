{
  "name": "01som",
  "version": "0.1.0",
  "description": "0 + 1 = SOM",
  "scripts": {
    "sass": "sass --sourcemap=none --no-cache --watch 01:01 --style compressed",
    "server": "http-server -c-1 .."
  },
  "private": true
}
